import type {BusinessAnalysis} from '../types';
export async function analyzeContent(name:string,content:string,source:string):Promise<BusinessAnalysis>{const base:BusinessAnalysis={businessName:name,services:[],targetAudience:[],onlinePresence:[],strengths:[],weaknesses:[],missingFeatures:[],painPoints:[],opportunities:[],recommendedServices:[],facts:[],sources:[source]};if(!process.env.GEMINI_API_KEY){return{...base,onlinePresence:[`Source available: ${source}`],facts:[{text:`Information was obtained from ${source}.`,evidence:'verified',source}]}}const prompt=`Analyze business ${name} using ONLY this source content. Extract services, audience, online presence, strengths, weaknesses, missing features, pain points, opportunities and recommended services. Mark facts vs inference. Content:\n${content.slice(0,30000)}`;const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${process.env.GEMINI_MODEL||'gemini-3.7-flash'}:generateContent?key=${process.env.GEMINI_API_KEY}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({contents:[{parts:[{text:prompt}]}]})});if(!r.ok)throw new Error(`AI analysis failed (${r.status})`);const j=await r.json();const t=j.candidates?.[0]?.content?.parts?.[0]?.text||'';try{return JSON.parse(t.replace(/^```json\s*/,'').replace(/```$/,'').trim()) as BusinessAnalysis}catch{return base}}
