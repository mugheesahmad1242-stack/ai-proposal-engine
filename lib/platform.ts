export type Platform='Website'|'Instagram'|'Facebook'|'LinkedIn'|'TikTok'|'Google Business'|'X'|'YouTube'|'Other';
export function detectPlatform(url:string):Platform{try{const h=new URL(url).hostname.toLowerCase();if(h.includes('instagram'))return'Instagram';if(h.includes('facebook'))return'Facebook';if(h.includes('linkedin'))return'LinkedIn';if(h.includes('tiktok'))return'TikTok';if(h.includes('google'))return'Google Business';if(h.includes('youtube'))return'YouTube';if(h.includes('x.com')||h.includes('twitter'))return'X';if(h.includes('http'))return'Website'}catch{}return'Other'}
